{{#partial "navbar"}}
    <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
    <li class="nav-item"><a class="nav-link" href="/heroes/new">Recruit a New Hero</a></li>
    <li class="nav-item"><a class="nav-link" href="/squads/new">Create a New Squad</a></li>
    <li class="nav-item active"><a class="nav-link" href="/heroes">View Heroes and Squads</a></li>
{{/partial}}


{{#partial "content"}}
    <h2 class="text-center">Table of Gathered Information on the Heroes and Squads</h2>
    <div class="row">
        <div class="col-md-6 text-center">
            <h5 class="text-center"><u>Heroes</u></h5>
            {{#if allHeroes}}
                <div class="card heroSquadCard">
                    <ul class="list-group list-group-flush">
                        {{#each allHeroes}}
                            <li class="list-group-item">{{name}} - <a href="/heroes/{{heroID}}">View Details</a></li>
                        {{/each}}
                    </ul>
                </div>
            {{else}}
                <p>No Hero registered at the moment</p>
            {{/if}}
        </div>

        <div class="col-md-6 text-center">
            <h5><u>Squads</u></h5>
            {{#if allSquads}}
                <div class="card heroSquadCard">
                    <ul class="list-group list-group-flush">
                        {{#each allSquads}}
                            <li class="list-group-item">{{name}} - <a href="/squads/{{squadId}}">View Details</a></li>
                        {{/each}}
                    </ul>
                </div>
            {{else}}
                <p>No Squads registered at the moment</p>
            {{/if}}
        </div>
    </div>

{{/partial}}

{{> layout.hbs}}